# Capabilities — as-built progress

**Purpose:** What exists vs what remains for each product capability.  
Target design: [`specs/`](specs/). Session priority: root
[`CURRENT.md`](../CURRENT.md). Product docs: [`manual/`](manual/) — **shipped
only**.

**Update this file in the same change** whenever status or gaps change.  
See [`progress-model.md`](progress-model.md).

**Status vocabulary:** `shipped` · `partial` · `spec’d` · `planned` · `idea` · `retired`

**As of:** 2026-09-04 (terminal first tab `$HOME` when no source cwd; kvm Linux listen Super+Tab confirm is key-before-modifiers, unsmoked; kernel auto-repeat not injected; sola-spotify: clicks re-activate after Connect inactive; session teardown waits before reconnect; `/me` 429 no longer 2s-polls; artist/album name links; album Save/Follow + artist Follow — **installed** `spotify` release; mail send identities **installed** `mail`+`settings` release; browser: vault stay-logged-in + Log out, HTTP auth persist, helper Shutdown-flush, Google passkey document-start intercept, ⌘⇧R hard reload — **installed** `browser` release, desk smoke pending; mail attachments send/receive; kvm Linux listen + CLIP1 text/`image/png`; spectrum/stats raster (not iced 1×1 canvas); workspaces `workspace.set --name` moves `.worktrees/<slug>` + `--branch`; self-cleanup: `workspace.rm --worktree` + gone-path reap; grid selection follows scroll; idle omnibox query-strip + Kagi lucky Enter + page ⌘V **smoked**; switcher count marks + grouped notify pile, no cap 20 — **installed** `kit`+`shell` release; shell menubar pixel-graph stats + volume 12-band LED spectrum + notify pile bell + count + freeze-first Super+Shift+4 **installed** `shell` release; Super+K overlay Frames to the card; DevTools docked panel **smoked**; JS dialogs kit modal; sola-spotify **on master** **installed** `spotify` release; number pad digits **smoked**; Slack huddle camera **smoked**; mail move-rules on connect + From/To equals vs `Name <addr>`; settings rules list+detail + dest select; browser tab favicons + `window.open` / NEW_POPUP as a focused tab; Window menu + Super+K overlay **installed** `kit`+`shell`+`paint`+`terminal`; mail dest-UID undo + compose table on master; shell Super+H hide + river dead-pid prune on master; wrapper Edit/links/notify/huddle on master; browser codecs CEF + autoplay + vault/⌘G; shell Bluetooth + volume; `crates/sola-agent` retired; notifications HUD on master; `sola-scope` on master; `ipc-compat` idea parked)

**Manual column:** `yes` = may document as fact · `partial` = limited honest
docs · `no` = do not present as product · `n/a` = engineering-only.

---

## Platform core

| ID | Capability | Status | Spec / plan | Dogfood | Gaps | Manual |
|----|------------|--------|-------------|---------|------|--------|
| supervisor | Process manager launches/restarts components | shipped | architecture | local TTY | Policy for crash loops / backoff polish | partial |
| bus | Unix-socket event bus + stickies | shipped | [persistent bus](specs/2026-04-24-persistent-bus-design.md) | local | Sticky surface still expanding; see bus freezes. Mixed worktree installs can silently remap `TopicKind` (postcard `$subscribe`) — parked as [ipc-compat](ideas/2026-08-30-ipc-contract-compat.md) | partial |
| ipc-compat | Runtime bus/call contract handshake; mixed binaries fail with exit 76 | idea | [idea](ideas/2026-08-30-ipc-contract-compat.md) | — | Not implemented. No preamble; subscribe decode failures are swallowed; PM restarts every non-zero exit | no |
| call | Request/reply host + `solactl compositor`/`session` + kit helper | partial | [call plane](specs/2026-08-13-sola-call-plane-design.md); [workspaces CLI](specs/2026-08-18-workspaces-cli-design.md) | code; `call` installed 2026-08-21 with monitor | **Gaps:** no MCP; confirm is **D3**; no catalog sticky on the bus (monitor uses `Role::Observer` + `Wire::Trace` / `Catalog` instead); Workspaces `workspaces` methods implemented (desk smoke pending); `MethodSpec.timeout_ms` advertised; `LaunchResult` still a bus reply | partial |
| bus-reconnect | Apps survive bus restart | shipped | seamless restart freezes | local (menubar framed after restart) | Broader app-menu re-publish edge cases | no |
| river-bridge | sola-river ↔ River Wayland | shipped | [river design](specs/2026-04-16-sola-river-design.md) | local | NumLock on by default (`river-xkb-config-v1` on each keyboard) **installed** `river` release 2026-09-01; pad digits **smoked**. Layer-shell / exclusive focus quirks; skip re-`focus_window` when already focused (FFM flash mitigation). Dead-pid prune after SIGKILL (no `closed`) **installed** `river` debug 2026-08-31 | partial |
| session-mgr | sola-session spawn/close/reap | shipped | [session](specs/2026-04-17-sola-session-design.md) | local | Persistence depth varies by app | no |
| theme-bus | Topic::Theme palette + fonts | shipped | [sidebar/theme](specs/2026-05-07-sidebar-and-theme-protocol-design.md) | local | Full theme editor UX incomplete | partial |
| kit | sola-kit components + storybook | partial | [kit](specs/2026-04-30-sola-kit-design.md), graphite DS, [unified sidebar](specs/2026-08-13-unified-sidebar-design.md); [Window menu](specs/2026-08-31-window-menu-and-shortcuts-design.md) | storybook + apps; FilePicker installed with paint; overflow chips only when `section_scroll` is wired | **Window menu** helper (`menu::window_menu` / `BusSetup::window_menu`); shell injects if omitted. Select popover. Storybook desks (`pages/chrome`). **FilePicker** (720px modal, breadcrumb chips, Places; first consumer: paint). **Prose** (14px reading role + letter renderer; drag-select + inline links; first consumer: mail). **JSON** highlighter (theme-token spans; first consumer: monitor). **`list_item`** is graphite lift (`background.strong`), not the selection atom / dark-cyan wash. **`toolbar_icon_tip`** (muted SVG + no tip when disabled). Overflow chips only when `section_scroll` is wired. Collapsible sections are an **inset pocket**; list etch **reserves** the 1px lip. **`SidebarPanel` gesture** is kit-owned (`sidebar::State` + `Event::{Activate,ToggleSection,Drop,Resize}`). **`SidebarItem::leading`** is a reserved 16px slot (browser favicons). **ReorderStrip** ([2026-08-23](specs/2026-08-23-sidebar-reorder-strip-design.md)): Morph2 hole + FLIP (Scratch `morph2.js`); sola-browser tab strip uses it. Working ring uses `RedrawRequest::At` (~20 Hz), not vsync `NextFrame`. **`floating_frame`** clips the inner face to a rounded rect (all four corners) so full-bleed content cannot square the bottom ears — **installed** `kit` release 2026-09-02 (storybook). Gaps: no multi-select; no type-to-filter beyond name field; terminal not on Morph2 yet; other apps need their own install for the clip | no |
| install-make | cargo make build/install to /opt/sola | shipped | — | local | Local `/opt/sola` only (no remote host). Install requires human permission | yes |
| fonts-dist | System fonts only; distribution notes | shipped | [manual/distribution](manual/distribution.md) | local | Optional families must be installed by hand | yes |
| dist-shape1 | NixOS module + release tarball install | partial | [INSTALL.md](../INSTALL.md), [freeze](specs/2026-08-05-distribution-image-design.md) | colleagues historically | Published tarball URL **404** for v0.1.1; needs `cargo make publish`. Module option `services.sola.installRelease` (default true) gates the tarball. From-source path: [`CONTRIBUTING.md`](../CONTRIBUTING.md) with `installRelease = false` (River, nix-ld, fonts, wayland/xwayland; cargo owns `/opt/sola`) | partial |
| dist-vm-image | QEMU qcow harness (`cargo make vm`) | shipped | [freeze](specs/2026-08-05-distribution-image-design.md), [plan](plans/2026-08-05-distribution-qemu-image-plan.md) | QEMU install+boot target | Engineering only — not product media; stage always `target/release` | no |
| dist-installer | Flower splash + kit wizard + disk apply → loginless Sola | partial | [freeze](specs/2026-08-05-distribution-image-design.md), [plan](plans/2026-08-05-distribution-qemu-image-plan.md) | QEMU **vdb** e2e on master | **Gaps:** ISO e2e dogfood; TZ auto-detect (interim US/Mountain); polish; no `docs/manual/` ISO guide yet | no |
| dist-iso | Installer ISO build/run (`cargo make iso`) | partial | [freeze](specs/2026-08-05-distribution-image-design.md), [plan](plans/2026-08-05-distribution-qemu-image-plan.md) | build/run scaffold | Same live stack as qcow; multi‑GiB; **Gaps:** signed-off ISO→erase→reboot→Sola pass | no |

---

## Shell & windowing

| ID | Capability | Status | Spec / plan | Dogfood | Gaps | Manual |
|----|------------|--------|-------------|---------|------|--------|
| shell-menubar | Top menubar, menus, clock/stats | shipped | [shell iced](specs/2026-05-22-sola-shell-iced-port-design.md); [system monitors](specs/2026-06-16-menubar-system-monitors-design.md); [Bluetooth](specs/2026-08-29-shell-bluetooth-menubar-design.md); [volume](specs/2026-08-29-shell-audio-menubar-design.md); [Window menu](specs/2026-08-31-window-menu-and-shortcuts-design.md) | local; Bluetooth + volume `shell` installed debug 2026-08-29; BlueZ on (AX210); WH-CH520 paired | **Flower:** Restart Computer / Shut Down (logind; no confirm; not in Super+K; **installed** `shell` release 2026-09-03, unsmoked; toast on fail). **Window** menu injected for every focused app (kit default; app may replace). **Bluetooth:** nearby omits 6-pair hex addresses; no Forget/unpair. **Volume:** 12-band LED spectrum is the chip (no speaker glyph; click the bars; FFT of the monitor; phosphor stack; Bluetooth sits to its left with the same phrase gap as CPU; punch gate so the bottom two LED rows stay dark until a real peak; **installed** `river`+`shell` release 2026-09-02; Spotify desk-smoke: presence-band ~55 Hz–6.5 kHz, peak-hold autoscale, pink tilt); output/input pick via `wpctl`; chip highlight requires the popover to be open; volume-keys vs chip / sink switch still pending; no per-app streams. Mail unread chip. Right-cluster extras/percents/rates/clock group as phrases; CPU/GPU/MEM/RX/TX + volume spectrum are btop-style pixel graphs **rastered to a wgpu image** (2026-09-03; iced canvas 1×1 rectangles drop on GLES2 / llvmpipe). Visual polish vs macOS roadmap. Window menu **installed** `kit`+`shell` debug 2026-08-31 | partial |
| shell-launcher | App launcher | shipped | shell design | local | Search ranking / recents polish | no |
| shell-shortcuts | Super+K keyboard cheatsheet overlay | partial | [Window menu + Super+K](specs/2026-08-31-window-menu-and-shortcuts-design.md); [manual](manual/sola-shell.md) | **Installed** `kit`+`shell`+`paint`+`terminal` debug 2026-08-31; card-sized live Frame **installed** `shell` release 2026-09-02 | Lists built-in shell/window/capture chords + focused-app menus; click/Enter runs the action. Flower **Keyboard Shortcuts**. Live Wayland surface is the card + shadow pad (~0.3M px vs ~2M at 1920×1052); leftover pad dismisses; desk clicks pass through (menu pattern). Gaps: desk smoke of the card Frame; no remapping; no zone checkmarks on the Window menu | partial |
| shell-switcher | MRU switcher | shipped | [switcher](specs/2026-04-09-switcher-design.md); [notifications](specs/2026-08-25-sola-notifications-design.md) | local; count mark **installed** `kit`+`shell` release 2026-09-03 | Count mark on the icon (unseen live + pile; Mail unread; bell/focus acks). Gaps: desk smoke; post-confirm FFM holdoff unmerged | partial |
| shell-ffm | Focus-follows-mouse (dwell, no raise) | shipped | river + shell iced | local | Title-only Windows + Composition/chord dedup on `naturalethic/focus-flashing` (hygiene). Reported “Orca panel flash” reclassified to Orca/Grok pane UI, not shell | no |
| shell-zoning | Zone assignments | partial | zones + floating freezes; [Window menu](specs/2026-08-31-window-menu-and-shortcuts-design.md) | local | Opt-in snaps + restore; unassigned windows no longer force full frame. Mouse path: Window menu (**installed** `shell` debug 2026-08-31). No live checkmarks for the current zone | no |
| shell-float | Floating windows | partial | [floating](specs/2026-06-24-floating-windows-design.md); [D2 titlebar](specs/2026-06-30-floating-windows-phase-d2-titlebar-design.md) | local | **Default for unassigned windows** (app size + `WindowFloating`); Super+drag **removed** (operator unused; ⌘-click reaches clients); kit CSD (`FloatState` + `floating_frame`) on monitor, settings, preview, mail, terminal, workspaces, kit storybook, browser — titlebar drag/resize is the float move path. **Face is a rounded rect on all four corners** (dest-out punch; iced `clip(true)` is AABB-only) so full-bleed content cannot square the bottom curve. Resize AABB corner pads kept for hit-test. **Installed** `kit` release 2026-09-02 (storybook). **Gaps:** desk smoke; other float apps need their own install to link the new kit | partial |
| shell-opening-toast | “Opening …” menubar whisper | shipped | — | local | — | no |
| shell-notify | Desk notification cards + missed pile | partial | [notifications](specs/2026-08-25-sola-notifications-design.md) | KenHerbert Allow → displayed + screen top-right desk card (wrap no Native ctor; origin slash canonicalized). Installed `browser` 2026-08-27. Pile: bell + count, no Clear, overlay grows to usable height — **installed** `shell` release 2026-09-03. Grouped by app (≤4 list as rows so the bell matches; 5+ collapse; tag replace in pile; group ×) + Super+Tab unseen count mark (bell/focus acks) — **installed** `kit`+`shell` release 2026-09-03. | Inject `active_if_bound()`; Subscribe prefix + skip-unknown. **Gaps:** no sound / actions / D-Bus; grouped pile + switcher marks desk smoke | partial |
| shell-custom | Shell chrome tokens | partial | [shell customization](specs/2026-06-06-shell-customization-design.md) | storybook Shell page | Not all chrome uses tokens | no |
| app-hidden | Super+H hides the focused app (`Topic::AppHidden` → River `hide`, not send-to-back). Restore: Super+Tab, launcher (already-running). No menubar chip | partial | — | on master; **Installed** `river`+`shell` debug 2026-08-31; menubar remapped after SIGKILL zombie prune | Window menu Hide **installed** `shell` debug 2026-08-31. **Gaps:** only app_id key (Steam id variants); no animation | yes |

---

## First-party apps

| ID | Capability | Status | Spec / plan | Dogfood | Gaps | Manual |
|----|------------|--------|-------------|---------|------|--------|
| arcade | sola-arcade Steam library + windowed gamescope nest | partial | [manual](manual/sola-arcade.md) | **installed** 2026-08-25; unit tests; library JSON cache + bg rescan; A–Z/Recent (**persisted** `arcade-prefs.json`); ready-to-play filter; lazy viewport banners; Install on uninstalled; Stop-on-row; scroll preserve; nest steam exit; host label; zone/float + Cinema exit; `-S fit`; per-title nest **Fit to window** vs locked resolution (default 1080p; list up to native); **Fit follows host** `WindowGeometry` (nested mode-control + focused window 0,0); Factorio rezone dogfooded; nest `--cursor-scale-height` keeps the host pointer desktop-sized. **2026-09-01:** debounced `steamapps/` watch; session end is `UserAppExited` (no 1s `/proc` poll); Stop = `CloseApp` + Arcade-owned pids only; singleton raise (`arcade.lock.sock`); Play refused while desktop Steam is open | **Gaps:** Arcade UI must stay open for follow; Factorio must stay fullscreen; titles that ignore RandR still letterbox; some titles no host/crash; residual flicker; no `-e`; multi-store; never-played owned without local activity; cursor-scale not yet desk-smoked; ready-to-play filter not persisted; VDF/decode bounds still idea | yes |
| settings | Settings panel (theme, apps, mail cfg, …) | partial | [settings](specs/2026-04-19-sola-settings-design.md), apps list-detail; [wrapper](specs/2026-08-21-sola-wrapper-design.md) | local | Applications detail can create/edit a **web wrapper** (`kind` + `url`; command synthesized). Old 4-field Application postcard still decodes. Mail accounts are a list (inbox + extra SMTP identities) with send-from: inbox is a Wicket host checklist (no freeform; catch-alls omitted); extra accounts still type aliases; typing a Google Workspace address fills Gmail IMAP/SMTP via MX; Default From; rules are list + one detail (add/edit, dirty-lock); move destination is a kit select of Archive / Junk / Trash / Sent / Drafts (custom dest still offered). **Edit** menu Cut/Copy/Paste/Select All (Super+X/C/V/A — shell-routed `MenuAction` into the focused field). **Gaps:** dest list is canonical mailboxes, not live IMAP LIST; not all pages kit-parity | no |
| terminal | sola-terminal panes/tabs/tmux | partial | [terminal iced](specs/2026-06-03-sola-terminal-iced-port-plan.md) | local; first tab `$HOME` when no source cwd | Tab strip uses kit etch at **Large** density (reorder + 1…9 kept). Grid selection is kit neon `accent` @ 55% (not the graphite `selection` atom / dark-cyan mix). The wash is glued to buffer cells (moves with `display_offset` scrollback) and re-anchors when a TUI CUP-rewrites the live grid (`sel_follow`; Workspaces PTYs share this). Tab restore defers sticky `TerminalSession` until iced bus pump live (restart was empty UI with live tmux); publishes kit **Window** menu (**installed** `terminal` debug 2026-08-31). pane UX depth; links polish | no |
| browser | sola-browser chrome + CEF (single crate) | partial | [cef port](specs/2026-05-04-cef-port-design.md); [profiles D8](specs/2026-08-10-sola-browser-profiles-design.md); [create login](specs/2026-08-13-sola-browser-vault-create-login-design.md); [unified vault](specs/2026-08-28-sola-browser-vault-panel-design.md); [downloads](specs/2026-08-14-sola-browser-downloads-design.md); [tab groups](specs/2026-08-15-sola-browser-tab-groups-design.md); [notifications](specs/2026-08-25-sola-notifications-design.md); [manual](manual/sola-browser.md) | local: one iced window (`app_id=sola-browser`); **default web open target** (`sola_core::open_url`, `solactl open`, MIME via `sola-browser.desktop` for http(s), HTML, XHTML, `about:`, unknown; local HTML path → absolute `file://`, not `https://apocrypha/…`; no Helium/other fallback); **one chrome** (`chrome.sock` handoff; helper death respawns); outside open (`xdg` / bus / `solactl`) **raises** the existing window; per-profile headless `--engine` helpers; instant Profiles switch (menubar + kit identity select in the full-width chrome bar); page area blanks until a same-size frame for the new profile; chrome parks last composites per tab so a visited tab/profile switch is instant (miss blanks, never keeps the previous page); parked helpers keep live tabs (no reload); wrong-size last-frames are not stretched; YouTube persists after full quit; codecs CEF (H.264/AAC `canPlayType` `probably`) + `--autoplay-policy=no-user-gesture-required` **installed** 2026-08-30; helpers use live widget size; ARGB CPU paints swizzled to BGRA; Bitwarden **unified vault** (one toolbar icon; search + type chips + full records including notes/identities/cards; Fill on URI-matching logins; org vaults after sync); **Create login** (personal vault only; **+**; generate + POST cipher then fill); page ⌘C (selection via JS → helper IPC → Wayland clipboard) and in-page Copy buttons (`writeText` / `copy` event hook → same pipe); ⌘V (data-control read: image `File` paste or **once** `PasteText` in the focused field; iced text fallback restores the offer); newlines kept on the clipboard; tab-strip **favicons** (16px reserved leading slot; CEF PNG; muted globe until it arrives; empty on blank; **smoked** 2026-09-01); ⌘-click dogfooded (IMDb): Super+drag **removed**; JS href → chrome background tab below current (same group); `window.open` with features (`NEW_POPUP`) / `about:blank` is a windowless CEF tab (so the site still gets a `Window`) that chrome focuses beside the opener (**smoked** Cloudways DB/SSH); `target=_blank` is a focused tab; no native OS popup windows; ⌘T / xdg-open / `solactl open` append loose at the bottom; **page context menu** (CEF `run_context_menu` cancelled — no empty OSR strip; kit menu: open/copy link, edit, back/forward/reload); **hold back/forward** shows session history (`history.go` / LoadUrl after restore); tab history persists in `session.json`; kit context menu is 200 px wide (Fill-in-Shrink no longer collapses to a strip); OSR `click_count` 1/2/3 (triple-click line select); WebAuthn **get** on Google and Gemini Exchange 2FA (document-start V8 eval + all-frames + same-site coalesce; Google “Verifying it’s you” unsmoked); WebAuthn **create** (vault confirm; new login or attach; not yet dogfooded); OSCrypt `--password-store=basic`; OSR click focuses host + unfocuses iced chrome; punctuation (`.`, etc.) mapped to OEM VKs; CEF `OnLoadingStateChange` / progress drive back/forward/stop and a thin omnibox load line; **copy URL** button just left of the omnibox (committed page URL; check flash; disabled on blank); omnibar unfocuses on submit and keeps the committed URL (no mid-nav blank); tab close is chrome-authoritative (Tick merge does not resurrect the row); chrome stays interactive under animated tabs (shader `request_redraw` pump, not 60 Hz view rebuild; parked helpers `SetFront(false)` / hidden; frames on a dedicated socket, dirty-rect CPU/GPU path); OSR IME (page requests compositor IME; preedit/commit → `ImeSetComposition` / `ImeCommitText`; caret box via `FromEngine::ImeCaret`); Shift+wheel → horizontal; `<select>` `PET_POPUP` blitted onto the view (dogfooded); tab strip **drag-reorder** via kit `SidebarPanel::reorderable` (order persisted in `session.json`); titles fill the column (width-aware ellipsis, not a 20-char cap); **tab groups** (kit inset pocket + nested members + lucide header; reserved etch lip so selected titles do not shift; loose tabs below; **⌘⌥G** new group on a loose tab (name focused+selected; `⌘G` is Find Next) + hover pencil to rename + **edit-mode color swatch + check** (pocket fill, kit picker, luminance ink, hue-preserving lift + 1px lip on pockets and loose selected, persist hex / default well) + drag join/leave; no strip right-click; persist in `session.json`; visual polish **installed**; pocket color **installed** `kit`+`browser --release` 2026-08-31); **downloads** (CEF `DownloadHandler` → toolbar icon + progress; flat panel + middle-ellipsis names; auto-save `~/Downloads`; persist `shared/downloads.json`; dogfooded); getUserMedia kit Allow / Block (`media.json`); **`alert` / `confirm` / `prompt` / leave-page** kit modal (CEF `JsDialogHandler` → chrome overlay); idle omnibox is a label until click / `⌘L` (no query string; clipped); typing searches visit history (top 5 tray, same width as the field; ↓/`Ctrl+N`; Enter = highlighted row or Kagi lucky / first result, or switch to an open tab for a URL; scratch blanks discarded; Shift+Enter = Kagi results; `shared/history.json`); idle-bar ⌘V pastes into the page — query-strip / lucky / page paste **smoked** (`browser` release); Find `⌘F` / `⌘G` / `⌘⇧G`; New Group is `⌘⌥G`; **⌘⇧R** hard reload (CEF `reload_ignore_cache`; also Shift-click Reload); Bitwarden **stays unlocked** across restarts (`vault-session.json`); **Log out** wipes the session; HTTP Basic/Digest kit **Sign in** persists per profile (`http-auth.json`); **Developer Tools** docks under the page (bundled frontend over `/json/list`; dual OSR paint; **smoked**; brief black flash). | **Gaps:** Google passkey picker (document-start intercept) unsmoked; DevTools open/close flash; Find / JS-dialog-after-Continue-fix / ⌘⇧R hard-reload / vault stay-logged-in / HTTP-auth persist desk smoke; SVG-only favicons stay globe; group pocket color desk smoke; Steam store DASH trailer desk smoke (codecs CEF live: `canPlayType` AAC/H.264 `probably`); page menu has no save-image; history menu is session-only; no save-as / show-in-folder / delete-file; passkey **create** smoked (vault confirm; new login or attach); attach replaces any existing FIDO2 creds on that login; no PRF/hmac-secret; CEF CPU readback on NVIDIA is the accepted per-frame cost (not a slice); no create-card; no item edit; org-vault fill + identity/notes desk smoke; create login still personal-only; manual still limited | yes |
| agent | sola-agent ACP + Grok leader GUI | retired | [acp runner](specs/2026-07-23-sola-agent-acp-runner-design.md) (history), [ui backlog](specs/2026-07-23-sola-agent-ui-backlog.md) | unused since 2026-07-25; crate deleted 2026-08-28 | **Removed.** Iced ACP client is gone (no stub, no launcher row). Daily agent work is **Workspaces**. Leftover `/opt/sola/bin/sola-agent` until a later install. Do not rebuild. | no |
| workspaces | sola-workspaces: project groups + workspaces + agent-aware PTYs | partial | [freeze](specs/2026-08-13-sola-agent-terminal-design.md), [CLI](specs/2026-08-18-workspaces-cli-design.md), [idea](ideas/2026-08-12-sola-agent-terminal.md), [call plane](specs/2026-08-13-sola-call-plane-design.md) | on master; rail, splits, drop-project, dead-pane, `×N` smoked. `solactl workspaces` implemented (unsmoked). Add project expands `~`; groups stack at the top; no agent label on the workspace row. Sibling hover close is kit `on_close` (×). **Drop Project** menu-only (`project.rm`); worktrees stay. ⌘W closes the focused pane. Kit splits (⌘⇧↓ / ⌘⇧→) stay in the grid; the rail keeps one row per workspace. The mark rolls up Grok panes in that tab (waiting / needs-attention beats working beats done beats idle; shells do not contribute). Dead last pane: **Start new shell**; split leaf exit retracts. Switching a split attaches every leaf; hover does not spawn. Quiet `×N` on the workspace row is the loudest Grok session (session dir segments/checkpoints, then `signals.json` — that field can stay 0). Restart attaches only when `SOLA_WS_PATH` / cwd matches; leftover sessions quarantined. Shell builtin **Workspaces** (`lucide/folders`). Working ring spins (kit ms phase, ~20 Hz `At`; no `window::frames()`). Control plane: `ps` / `project.{list,add,rm,startup}` / `workspace.{list,spawn,set,rm,select,exec}` / `pane.{list,send,read,wait}` / `whoami`. `workspace.set --name` slugs the rail and `git worktree move`s to `.worktrees/<name>` (id stays; `--branch` is `git branch -m`). Per-project startup script after sibling spawn (Project menu). Lists include path/kind/parent. Spawn takes `--prompt-file`; parent defaults from `$SOLA_PANE_ID`. CLI spawn is background unless `--select`. Skill `sola-workspaces-cli` maps review/work ticket / create worktree / tell a pane onto spawn/exec (no rail steal). Workspace names prefer the Grok leaf. Lead Grok hooks reclaim the pane after session rotation (`/new`, `grok -r`); SessionStart idles a leftover working ring; `StopCancelled` → done; child `subagentType` ignored. `workspace.rm` / `project.rm` reply then teardown (self-close does not hang `solactl`). `workspace.rm --worktree` also `git worktree remove`s (`--force` to toss dirty); a gone checkout reaps the tab (no leftover working spinner). Hover × / plain `workspace.rm` leave the folder. `pane.send` / `workspace.exec --prompt` tmux bracketed-paste then Enter. Unfocused desk cards: title `{project} · {tab}`, body `grok is done` / `needs attention`. Super-chord no longer latches LOGO (⌘T/⌘V used to drop every later key until quit). Exiting Grok to the shell idles the mark (grey disc). Grid selection follows the glyphs when the pane scrolls (tmux/Grok CUP rewrite or local scrollback; shared `sola-terminal` `sel_follow`). | **Gaps:** `solactl workspaces` desk smoke pending; no UI rename modal / recolor / reorder; CLI `workspace.set --name` moves the worktree (unsmoked); Claude presence-only. | partial |
| mail | sola-mail kit client | partial | [mail kit](specs/2026-07-27-sola-mail-kit-design.md); [manual](manual/sola-mail.md) | local IMAP; **installed** `mail`+`settings` release 2026-09-04 (send identities + attachments) | Opens on the last inbox snapshot (not a Connecting splash); per-account connect status is a bottom-right card until every IMAP session is up; Letter pane (kit `prose`); HTML preferred; unread **bold**; always-on icon toolbar (message actions muted until a row is selected); scroll-to-load; graphite `list_item` selection; list pointer is the default arrow (I-bar + drag-copy stay in the letter); in-body drag-select + Edit Copy / Select All; first-party / magic-link URLs stay clickable (not dropped as tracking); IMAP `SELECT`+`FETCH` (no SEARCH); empty Junk/Trash batches + toasts; inbox unread on the shell menubar (`Topic::MailStatus`; hidden when mail is closed); **optimistic delete** (row gone immediately; MOVE runs on a second IMAP session so the selected letter loads without waiting); the reading pane **clears** until the newly selected body’s FETCH arrives (never shows the previous letter under a different row); combined list is newest-first by parsed date (RFC 2822 and IMAP `04-Sep-2026 … -0700`; list label never shows a zone); load-more only after a downward scroll (deletes do not look like a fetch of older mail); Gmail CREATE `Archive` on connect if the account has no Archive label; **undo** reverse-moves the destination UID (COPYUID or dest Message-ID scan) — never the source UID against Trash (that number is a different message); action toast auto-hides after 5s; compose headers are a caption-size label+field table at full pane width; **keyed list** keeps scroll and row identity after delete (silent refresh does not snap back to the first page); **IDLE on INBOX** (wake or 30s re-SELECT) + 45s poll; silent first-page merge drops UIDs the server no longer has (other-client MOVE/EXPUNGE); **move rules** apply on connect (newest 500 INBOX) and on IDLE (newest page); From/To `equals` / `address` match the address inside `Name <addr>`; **attachments:** list paperclip from IMAP `BODYSTRUCTURE`; reading pane Open (paint for images, browser else) / Save (kit FilePicker, default Downloads); compose Attach (toolbar paperclip, Message → Attach Files… ⌘⇧A, or Attach next to Send) as `multipart/mixed`; **send identities:** inbox `aliases` + extra SMTP `accounts` + `primary_from`; compose From is settings identities only (Default From first, then A–Z; unchecked aliases and catch-alls omitted — not the host’s full alias dump); compose From is a kit select when there is more than one; reply picks the identity in the original To/Cc; SMTP follows that From; typing the email fills IMAP and SMTP for Gmail / Google Workspace (MX → imap.gmail.com) / Outlook / Fastmail / Yahoo / iCloud / Proton; IMAP/SMTP enable checkboxes. **Gaps:** no HTML engine (CID images are files, not inline); no drag-drop onto compose; no forward-with-attachments; no full offline (list snapshot only); IDLE is INBOX-only; flag-only `\Deleted` without EXPUNGE waits for the 45s poll; undo dest-UID identification if COPYUID and Message-ID are both missing; connect apply is newest 500 only; IDLE is per-account INBOX only; write-session fallback shares the list connection if the second IMAP login fails | limited |
| monitor | sola-monitor bus + call inspector | partial | [call plane](specs/2026-08-13-sola-call-plane-design.md); [manual](manual/sola-monitor.md) | installed (`call` + `monitor`, 2026-08-21); on master with this merge; desk smoke pending | Kit sidebar (Bus / Call), `list_item` log, kit select + toolbar, hairline splits, inspector well, last-known stickies / live owners. Call plane via observer (`install_observer`). **Gaps:** desk smoke pending; follow-tail is a scroll-offset heuristic; no copy-from-inspector | partial |
| preview | sola-preview (argv / launcher image viewer) | partial | [preview](specs/2026-08-04-sola-preview-and-selection-capture-design.md); [image clipboard](specs/2026-09-01-image-clipboard-design.md); [manual](manual/sola-preview.md) | local; **installed** `kit`+`preview`+`browser`+`wrapper` debug 2026-09-01 | Header **Copy** writes image bytes (`image/png` via data-control); **Copy path** is text. Shell hotkeys no longer open Preview. MIME / `solactl open` stay on paint. **Gaps:** zoom; no single-instance; clipboard offer dies when Preview quits | yes |
| paint | sola-paint default image viewer/editor | partial | [paint](specs/2026-08-14-sola-paint-design.md); [manual](manual/sola-paint.md) | local (`paint` installed; tab persist needs reinstall) | Single-instance + zoom/pan + tab persist (`PaintSession`). Crop is **⌘⇧K** (⌘K is the shell shortcuts overlay; **installed** `paint` debug 2026-08-31). **Gaps:** no clipboard image; unsaved buffers not persisted; crop/rotate/flip/save only | partial |
| wrapper | sola-wrapper: website as its own Sola app | partial | [wrapper](specs/2026-08-21-sola-wrapper-design.md); [manual](manual/sola-wrapper.md) | **on master.** Slack paints. Edit, off-site links, desk notify, huddle OSR + mic **smoked** 2026-08-29 (`wrapper` debug). LifeCam huddle camera **smoked** 2026-09-01 (V4L2 `getUserMedia` 640×480 + 720p) | Argv `sola-wrapper <id>`; `app_id` = configured id; Applications `kind`/`url`; per-id CEF profile (`~/.config/sola/wrapper/<id>/`); CEF via sola-browser lib + `bind_external`; singleton per id; old 4-field Application postcard still decodes; **Edit** menu (⌘X/C/V/A via shell `MenuAction`; paste is compositor clipboard → image `File` or text); Web `Notification` → desk card (`app_id` = wrapper id; grants `…/wrapper/<id>/notifications.json`); off-site `target=_blank` / ⌘-click / `window.open` → sola-browser (`sola_core::open_url`); Slack huddle `about:blank` NEW_POPUP is a windowless CEF tab; `getUserMedia` → kit Allow / Block (`media.json`) + Chromium `MEDIASTREAM_*` content settings; JS `alert`/`confirm`/`prompt` kit modal (shared with sola-browser). Kit/session set `XDG_SESSION_TYPE=wayland` when TTY launch left `tty`. **Gaps:** image paste desk smoke (Preview Copy → Slack ⌘V); no Bitwarden/downloads/tab chrome; no `--url`; no PWA install; host user may need `video` extraGroup for non-seat tools | partial |
| scope | sola-scope pixel loupe | partial | [manual](manual/sola-scope.md) | **on master**; smoked 2026-08-26 (live follow; desktop pointer on, sprite omitted from grid) | Magnified grid around the pointer via `compositor.sample`; zoom 65×65…3×3; hex/RGB + copy; remembered float; wlroots copies overlay_cursor=0 before the software-cursor blit. **Gaps:** first output only; ~10 Hz while open; no freeze | partial |
| spotify | sola-spotify kit client (library, Connect, local playback) | partial | [manual](manual/sola-spotify.md); mined from [Fastpotify](https://github.com/crmne/fastpotify) (MIT) | **On master.** **Installed** `spotify` release 2026-09-04 (artist/album name links; album Save/Follow + artist Follow; `/me` 429 keeps the session and does not 2s-poll; clicks re-activate this computer after Connect goes inactive; reconnect waits for old Spirc teardown). Earlier 2026-09-03: Back/Forward **survives restart**, max 20 back steps; add-to-playlist picker; media keys immediate; page Back/Forward. Launcher row live. Standing OK to `install spotify` after each build. | Kit app `sola-spotify`: PKCE Web API sign-in (shared public client id), library rail, collection pages, wrapping album/artist/mix catalogs, player bar, Connect, local librespot (Premium), MPRIS (play/pause/next/prev apply as soon as D-Bus delivers them, not on the 2s playback poll; local engine state is published so the identity is not stuck Stopped). Page **Back / Forward** (chevrons at the top of the main view; ⌥←/→, ⌘[/], mouse side buttons; search query is part of the Search history entry; stack persists across restart, max 20 back steps). Home/playlist/artist fetches run in parallel; library playlists fill in after first paint. Spotify-generated playlists (id `37i9dQ…` / owner Spotify) are one Library item plus a catalog page; Home shows an 8-mix shelf with See all. Extra mixes from the Made For You browse category when the API allows. Playlist skip-to-row; context auto-advance updates the bar. Artist and album names on a track (list, queue, player bar) open those pages (hover underline; each credited artist is its own link). Player-bar cover opens the album. Album page **Save** / **Follow** (primary artist) and artist page **Follow** write the official library (`/me/albums`, `/me/following`); they show on the Albums / Artists tabs. **Selected** is graphite lift (last track restored); **playing** is a cyan play mark + accent title. **−** hides from autoplay (struck title, dim cover; does not delete). Liked Songs syncs `/me/tracks` into `liked.json` (contains on the shared app 429s); a cyan circled + marks a liked track. **list-plus** on the row and player bar opens an add-to-playlist picker (writable lists, last-used first, filter; **New playlist** creates a private list and adds the song). Playlist header + rows show added dates. Reopen last page + last track + Back/Forward stack. A shared-app 429 on `/me` keeps the saved session (does not kick to sign-in) and backs off profile poll for 5 min (was every 2s, which kept the bucket empty). Device list poll is 30s and skipped while cooling. Local Load/Play **activate** this Connect device first — librespot otherwise ignores the command while inactive. Reconnect waits for the old Spirc to delete connect state, delays the first Load until Spirc has registered, and retries if playback never starts. Tokens + `skipped.json` under `~/.local/state/sola/spotify/`; settings `~/.config/sola/spotify/settings.json` (includes last playlist + nav). **Gaps:** no playlist reorder/delete; no podcasts UI; no Winamp / MilkDrop / EQ; no tray / single-instance; no personal Spotify app quota (shared-app 429s); no lyrics; zeroconf speakers; local skip can still hitch; local-file tracks have no catalog page; librespot now-playing may lack album id until a Web API snapshot; album **Follow** is the primary artist only | partial |
| kvm | sola-kvm Linux server → Linux or Mac client | partial | [kvm](specs/2026-07-27-sola-kvm-design.md), clipboard | dual-host; Linux `listen` injects via virtual pointer/keyboard; CLIP1 text + `image/png` Linux↔Linux; Super+Tab confirm is key-before-modifiers; kernel auto-repeat is not injected (River press-count) | Permanent input ACL (server); Mac clip is text-only (PNG Ack-rejected); Mac scroll is CG velocity gain; Super+Tab confirm desk smoke | [kvm](manual/sola-kvm-operator.md) |
| solactl | Operator CLI (clap owners + live extras) | partial | [call plane](specs/2026-08-13-sola-call-plane-design.md); [workspaces CLI](specs/2026-08-18-workspaces-cli-design.md); [manual](manual/solactl.md) | code | `open` → sola-browser (http/s, HTML) or sola-paint (image path). Terminal `open` is `xdg-open` (`sola-browser.desktop`). Live `workspaces` owner: bool flags do not eat the next `--flag`; invoke timeout follows `MethodSpec.timeout_ms` or `--timeout`. **Gaps:** `eval` removed; screenshot/apps/input live under `compositor`; `solactl workspaces` desk smoke pending | yes |

---

## Cross-cutting / polish programs

| ID | Capability | Status | Spec / plan | Dogfood | Gaps | Manual |
|----|------------|--------|-------------|---------|------|--------|
| macos-look | macOS dark chrome density/materials | partial | [look roadmap](specs/2026-07-20-macos-look-and-feel-roadmap.md), [design language](manual/design-language.md) | local | Phase program incomplete | partial |
| design-language | Documented visual law | partial | [design-language](manual/design-language.md) | — | Not all apps comply | yes |
| screenshot | Capture + clipboard | partial | [screenshot plan](specs/2026-07-20-screenshot-capture-plan.md); [call plane](specs/2026-08-13-sola-call-plane-design.md); [preview](specs/2026-08-04-sola-preview-and-selection-capture-design.md); [image clipboard](specs/2026-09-01-image-clipboard-design.md) | local. Super+Shift+4 freeze-then-crop **installed** `river`+`shell` debug 2026-08-31 (smoked). `--app` toplevel scene copy (no raise) **installed** `river` debug 2026-08-31. Super+Shift+3/4/5 → promised `image/png` + Fastest encode **installed** `kit`+`shell` release 2026-09-01. Super+Shift+4 no longer dismisses menubar panels before the freeze (**installed** `shell` release 2026-09-02). | **Gaps:** occluded / Super+H-hidden `--app` smoke; multi-output; freeze-with-open-panel desk smoke | [shell](manual/sola-shell.md) / [solactl](manual/solactl.md) |
| doc-truth | Progress + manual match as-built | partial | [progress-model](progress-model.md) | — | Freeze headers not backfilled; vault stale | n/a |
| site | Public marketing site sola.computer | partial | [marketing PRODUCT](marketing/PRODUCT.md) | live teaser 2026-08-20 | Thoxa container on Wicket aulos (`wl-f2835fc5634e`); Paper Teaser only; ISO notify SQLite. **Gaps:** full Landing artboard not shipped; no ISO URL | no |
| gpu-idle | Iced present storms (timers / `window::frames` / `NextFrame` rings) | partial | [PERFORMANCE.md](../PERFORMANCE.md) · [architecture — Iced present / GPU idle](architecture.md#iced-present--gpu-idle-as-built) | local 5120×2160 NVIDIA; 2026-08-25 sample mean **17.7%** P8 / 21 W (was ~30–40%) | **Done:** no 16 ms chrome timer; `Tick` wakes from helpers; Working ring `At` ~20 Hz; workspaces pointer gated; Morph2 vsync **drag-only**; shell overlays **parked 2×2 off-output**; **menu overlay is card-sized** (not full-output wgpu); **Super+K overlay is card-sized** (same helper family; software-GL mouse lag); tiled kit **opaque-region** in patched `iced_winit`. **Gaps:** opaque-region not smoked (session apps did not re-exec); River NVIDIA knobs; first overlay after a fresh shell still maps the parked surface; launcher/switcher/selection still full-output; workspaces `BlinkTick` ~2 Hz; browser click still one present | no |

---

## How to edit a row

1. Change **Status** only with evidence (code and/or dogfood).  
2. Keep **Gaps** non-empty when Status is `partial` or `spec’d`.  
3. Set **Manual** to `yes` only if `docs/manual/` may describe it as available.  
4. Point **Spec / plan** at freeze or plan.  
5. If this was the active slice, also update `CURRENT.md` **Now** / dogfood.  
6. Policy forks → [`open-questions.md`](open-questions.md#decision-points-ask-human).

---

## Recently notable (compact)

| Area | Notes |
|------|--------|
| **sola-agent** | 2026-08-28: crate deleted. Launcher builtin **Agent** and settings `SYSTEM_APP_IDS` entry dropped. Workspaces is the agent product. ACP runner freeze kept as history. |
| wrapper | 2026-09-01: Slack huddle camera **smoked** (LifeCam V4L2 640×480 + 720p). `media.json` Allow also writes Chromium `MEDIASTREAM_*`. 2026-08-29: huddle OSR popup + mic Allow **smoked**; Edit, off-site links, desk notifications. On master with this merge. | 2026-09-02: JS `alert`/`confirm`/`prompt` kit modal (shared with browser).
| **sola-scope** | 2026-08-26: on master. Live pointer follow (River `pointer_position` on motion). Desktop cursor stays; loupe omits the sprite (wlroots precommit copy + under-cursor damage). Zoom 65×65…3×3; remembered float. |
| **sola-monitor** | 2026-08-21: Bus/Call planes; kit chrome; inspector well; call observer (`Role::Observer` + `Wire::Trace`). Merged from `monitor-polish`. |
| **river-bridge** | 2026-09-02: shell re-exec keeps app z-order (Composition stabilize when only the menubar id changed) **installed** `river`+`shell` release. 2026-09-01: NumLock on by default for every keyboard (`river-xkb-config-v1.numlock_enable` at attach) **installed** `river` release; pad digits **smoked**. Super+Numpad zoning still works with NumLock off. User can still toggle NumLock. |
| **app-hidden** | 2026-08-31: Super+H hides the focused app (`Topic::AppHidden` / River `hide`). Restore via Super+Tab / launcher. Menubar restore chip removed. Dead-pid prune after SIGKILL (no `closed`) **installed** `river`+`shell`. |
| **shell-menubar** | 2026-09-03: CPU/GPU/MEM/RX/TX + volume spectrum LED matrices **raster to a nearest-neighbor RGBA image** (not iced `canvas` 1×1 `fill_rectangle`; those tessellations drop on GLES2 / software GL). Same day: flower **Restart Computer** / **Shut Down** (logind `reboot`/`poweroff`; no confirm; omitted from Super+K; toast on fail) — **installed** `shell` release. NixOS module grants `wheel` polkit YES (no agent). Same day: notify pile groups by app (no cap 20; ≤4 list as rows; 5+ collapse; same-tag replace in pile); Super+Tab count mark is unseen attention (bell/focus acks, pile stays; Mail unread) — **installed** `kit`+`shell` release. Same day: mail unread + notify pile share glyph+count rhythm (bell shows pile count) — **installed** `shell` release. 2026-09-02: volume chip 12-band LED spectrum analyzer (no speaker glyph; click the bars; own phrase with CPU-sized gaps; punch gate; FFT of the default-sink monitor, phosphor stack, Bluetooth left of volume; presence-band ~55 Hz–6.5 kHz, peak-hold autoscale, pink tilt; Spotify desk-smoke) — **installed** `river`+`shell` release. Same day: CPU/GPU/MEM/RX/TX are btop-style dithered pixel graphs (numbers in the dropdown); right cluster groups extras/percents/rates/clock; app-menu dropdowns use laid-out label X; volume chip highlight tracks `menu_open` — **installed** `shell` release. 2026-08-29: Volume chip left of Bluetooth (`Panel::Audio`; `pw-dump`/`wpctl`; installed debug). Bluetooth nearby omits 6-pair hex (`AA-BB-…`). Bluetooth chip + popover installed debug; host BlueZ on (AX210); WH-CH520 paired. 2026-08-25: Flower **Quit Sola** has no Super+Q (that chord closes the focused app; menu click still shuts the session). 2026-08-20: GPU dropdown ranks top processes by SM util (processing %) as well as VRAM; VRAM list merges compute + graphics contexts |
| **workspaces** | 2026-09-03: `workspace.set --name` slugs the rail and `git worktree move --force`s to `.worktrees/<name>` (catalog id stays; restamps `SOLA_WS_PATH`); `--branch` is `git branch -m` (does not move the folder); project root cannot rename. Same day: `workspace.rm --worktree` closes the tab then `git worktree remove` (self-cleanup no longer leaves a spinning tab); gone `.worktrees/<name>` paths reap. 2026-08-28: unfocused done/waiting are desk cards (title `{project} · {tab}`, body `grok is done` / `needs attention`). 2026-08-27: `pane.send` / `workspace.exec --prompt` are tmux bracketed paste + Enter (clipped composer / wheel-to-submit). 2026-08-25: `workspace.rm` replies before killing tmux (self-close no longer hangs); SessionStart idles leftover working; cleanup is one shell. Same day: split workspaces stay one rail row; Grok mark rolls up across panes (waiting > working > done > idle); `×N` is the loudest Grok session in the tab. 2026-08-18: rail marks reclaim on Grok `SessionStart` / `UserPromptSubmit` (session rotation no longer freezes some tabs); `StopCancelled` → done; child `subagentType` hooks ignored. Same day: per-project startup script (Project → Startup Script…). CLI control plane freeze + implementation (`project.add`, `workspace.select`/`exec`, `pane.wait`, `whoami`, `--prompt-file`, richer payloads, Grok-leaf targeting). Earlier today: `×N` only on a Grok leaf (segments/checkpoints; `signals.json` can stay 0); leaf labels follow presence; switch attaches every split leaf (hover does not spawn). Ctrl-D on a split leaf retracts (Start new shell only on the last pane). Kit pane splits (⌘⇧↓ / ⌘⇧→); ⌘W close pane; Drop Project menu-only; leaf rows after split. Restart binds tmux by path (`SOLA_WS_PATH`); leftover / externally-deleted sessions quarantined. ⌘T spawn / ⌘N new project; working ring spins (kit ms phase). Launcher builtin; kit hover × on siblings (not root); Add project expands `~`; groups stack at the top; agent name off the workspace row. 2026-08-15: landed on master. 2026-08-14: renamed from sola-agent-terminal; owner `workspaces` (`solactl workspaces`); config `~/.config/sola/workspaces/` |
| **browser polish** | 2026-09-04: Google passkey “Verifying it’s you” intercept is **document-start** V8 eval (prototype + platform-authenticator advertisement) so Bitwarden’s picker opens instead of Chromium’s OSR-invisible sheet — **installed** `browser` release, not smoked. Same day: chrome start **Shutdown**-flushes CEF helpers (cookie store) before SIGTERM; helper death reopens chrome’s tab list (does not resurrect closed tabs from a stale helper snapshot). Same day: HTTP Basic/Digest is a kit **Sign in** (saved per profile in `http-auth.json`) so a restart does not re-prompt — not smoked. Same day: Bitwarden **stays unlocked** across chrome restarts (`vault-session.json`, age-encrypted); **Log out** on the unlocked card wipes tokens/keys/session — not smoked. Same day: **⌘⇧R** hard reload (CEF `reload_ignore_cache`; Browser → Hard Reload; Shift-click Reload) — **installed** `browser` release, not smoked. 2026-09-03: idle omnibox **omits query strings** (clip long paths); Enter on a search is Kagi **lucky** (highlighted history row still wins; Shift+Enter = SERP); idle-bar ⌘V pastes into the **page** (tracked omnibox flag; unmounted field no longer drops Edit) — all **smoked** (`browser` release). Same day: omnibox **visit history** (top 5 tray, same width as the field; no autocomplete; ↓/`Ctrl+N`; Enter = highlighted / Kagi lucky or **switch to an open tab** for a URL; scratch `about:blank` discarded; Shift+Enter = Kagi results) **smoked**. Same day: Developer Tools **docked bottom panel** **smoked** (bundled frontend over `/json/list` websocket; dual OSR paint; not a strip tab; brief black flash on open/close). JS confirm Continue no longer panics the helper. Same day: idle omnibox is plain text until click / `⌘L`; Find `⌘F` / next `⌘G` / previous `⌘⇧G` (CEF `find`); New Group is `⌘⌥G`. JS `alert` / `confirm` / `prompt` / leave-page is a kit modal (CEF OSR `JsDialogHandler` → helper IPC → chrome overlay; wrapper shares it). **Installed** `browser` release 2026-09-03. 2026-09-01: tab-strip **favicons** (kit 16px leading slot; CEF `download_image`; globe fallback) **smoked** (`browser` debug). Same day: `window.open` / `NEW_POPUP` (Cloudways DB/SSH consoles) is a focused tab beside the opener **smoked**; `window.open` still returns a Window (windowless CEF); `target=_blank` focuses; ⌘-click stays background; no native OS popup. 2026-08-31: tab-group **pocket color** (kit edit-mode swatch + check; luminance ink; quiet hue-preserving lift + 1px lip; compact picker (toggle chip, anchored past check); black/semibold titles on light fills) **installed** `kit`+`browser --release`. Persist `#rrggbb`/`#rrggbbaa`; default inset well. 2026-08-30: codecs-enabled CEF 147.0.10 (`proprietary_codecs` / Chrome ffmpeg) in `~/.cache/sola/cef-<ver>/` — Steam DASH AAC/H.264. Same day: CEF `--autoplay-policy=no-user-gesture-required` **installed** `--release`. Same day: **⌘G** wraps a selected loose tab in a group, focuses and selects the name; hover pencil to rename (Browser → New Group; strip right-click removed) — **installed** `browser --release`. Same day: unified Bitwarden panel **installed** `--release` (one toolbar icon; search + full records). 2026-08-28: ⌘⇧T reopens closed tabs (LIFO, 25, per-profile `session.json`). 2026-08-20: tab-group drag: flush members, hairline rim, header moves the whole pocket, **title drop is invalid**, per-well extra eases independently, origin hole matches iced 1.3 etch row height (not 32px `PANEL_ROW_H`). Loose is a section with no box. Bitwarden fill/cards/TOTP/passkeys load **all vaults** (org keys after sync; desk smoke pending); page context **Open Developer Tools** (console tab) + **Inspect Element**; HTML5 OSR drag/drop; passkey **create** smoked; `solactl open` raise restack. 2026-08-18: scheme-less localhost / loopback uses `http://` (not https); hover × follows the pointer after a tab close (kit `HoverClose`; no mouse-out). 2026-08-17: tab-group **pocket** (kit inset well + nested members; selected title no longer shifts); ⌘T / xdg-open append loose at bottom (⌘-click still beside). **⌘-click** dogfooded (IMDb); Super+drag removed; vault **authenticator**. 2026-08-15: tab groups + page chrome. Earlier: downloads, cards, passkey get/create |
| **sola-paint** | 2026-08-15: tab persist (`PaintSession`); singleton + zoom/pan; screenshots stay on preview. 2026-08-14: first-pass kit app; default MIME dest; crop/rotate/flip/save; left tab strip; kit FilePicker |
| **call plane** | 2026-08-21: observer role + trace fan-out (monitor). 2026-08-14: host; `solactl compositor`/`session`; kit `CallSetup`; shell screenshot via call; bus `Evaluate`/`CaptureScreen`/`Screenshot`/`Simulate*` removed |
| **sola-arcade** | 2026-09-01: `steamapps/` watch (1s debounce); Stop/Play from `UserAppExited` (no 1s `/proc` poll); Stop does not `pkill AppId=`; singleton raise; Play refused while desktop Steam is open. 2026-08-28: A–Z / Recent sort persists (`arcade-prefs.json`). 2026-08-26: nest `--cursor-scale-height` matches initial `-H` so Factorio-class X cursors stay ~36px on the host (gamescope-wl otherwise presents the fake-monitor cursor 1:1). 2026-08-25: Fit **follows** the gamescope host frame (rezone/float) via nested `GAMESCOPE_XWAYLAND_MODE_CONTROL` + focused window at 0,0 (not host `:0`; no `--force-windows-fullscreen` CLI — that aborted gamescope-wl). 2026-08-24: per-title nest **Fit to window** vs locked resolution (default 1080p; dropdown up to native; `arcade-nest.json`). 2026-08-08: banner list + nest; library cache + bg rescan; A–Z/Recent; ready-to-play filter; lazy viewport banners; Install on uninstalled; `--nested-steam` (no BPM); exit nested Steam on game quit; scroll preserve; Cinema exit; gamescope float 16:9 |
| **Distribution → master** | 2026-08-06: `sola-install`, Plymouth flower, qcow e2e loginless Sola, ISO scaffold |
| Float defaults | Unassigned windows default-float + kit CSD on first-party apps |
| Progress docs | 2026-08-05: CURRENT + capabilities + architecture spine |
| Bus restart | Menubar frame survives bus reconnect |
| Mail | 2026-09-04: send identities (inbox aliases + extra SMTP accounts + primary From; reply matches To/Cc) — **installed** `mail`+`settings` release. 2026-09-03: attachments send + receive (kit FilePicker; list paperclip). Dest-UID undo / compose table on master. |
| Spotify | 2026-09-04: artist/album name links; album Save/Follow + artist Follow; `/me` 429 keeps session (no 2s poll); clicks re-activate after Connect inactive; reconnect waits for Spirc teardown; MPRIS follows local engine — **installed** `spotify` release. |
