[package]
name = "sola"
version.workspace = true
edition.workspace = true

[dependencies]
sola-bus = { path = "../sola-bus" }
sola-core = { path = "../sola-core" }
tracing = "0.1"
libc = "0.2"
signal-hook = "0.3"
