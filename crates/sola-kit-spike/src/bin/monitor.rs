//! `sola-monitor-lab` — HTML/CSS monitor twin. Distinct identity.
//!
//! Run `crates/sola-kit-spike/target/release/sola-monitor-lab`
//! (or workspace `target/release/sola-monitor-lab`). Do not install.

fn main() {
    sola_kit_spike::monitor::run();
}
