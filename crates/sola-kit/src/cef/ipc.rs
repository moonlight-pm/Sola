//! JS↔Rust IPC bridge. Browser side of CEF's MessageRouter
//! corresponds to `window.cefQuery(...)` on the JS side.

// TODO(taskD1): MessageRouterBrowserSide handler dispatching to KitApp::on_js_command.
